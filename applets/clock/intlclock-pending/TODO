make mechanism calls async
update set-time button label to policy

remove the "Change Date + Time" menu item

on the locations tab: add a "current timezone" column

on the add location dialog: add a "Pick location" button next to the
location entry that brings up a location picker like the one from the 
weather applet, and fills in the timezone when selected
turn the timezone selection into an extra dialog too, using a similar
layout to s-c-d
